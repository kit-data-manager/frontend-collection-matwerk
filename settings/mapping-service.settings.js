//The backend service URL for the MappingService instance used by the frontend.
export const ajaxBaseUrl = "https://matwerk.datamanager.kit.edu:8095";

export const keycloak = undefined;
export const tags = undefined;

export const showServiceUrl = false;

//The app description used to customize the frontend, e.g., for a specific project with a custom title and subtitle.
export const appDescription = {
    "app-logo": "./images/mapping_service.jpg",
    "app-title": "Mapping Service UI",
    "app-subtitle": "Extract metadata and map it to json."
};
